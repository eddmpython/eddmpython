# {{PROJECT}} Agent Bootstrap

작업 시작 전에 `CLAUDE.md` 를 끝까지 읽는다. 규칙의 정본은 그 파일이다.
이 파일은 규칙을 담지 않으며, 충돌하면 `CLAUDE.md` 가 이긴다.

더 깊은 참조는 `{{DOCS_DIR}}` (지금 도는 계약) 과 `{{TRACKER}}` (예정) 에 있다.

도구 구성이 `AGENTS.md` 중심이면 두 파일의 역할을 바꿔도 된다. 그때는 규칙 본문을 이 파일에만
두고 `CLAUDE.md` 가 이 파일을 가리키게 한다.
