# 자동화 시작하기

**세트 1이다.** 카테고리 하나가 세트 하나이고 4~6편이며, 그 세트만 읽어도 일 하나가 실제로 돈다.
이 세트의 성과는 설치 없이 브라우저에서 지출내역 세 건을 분류하는 코드가 도는 것이다.


글을 쓰기 전에 `memory/blogWriter.md` 를 연다. 그다음 이 폴더의 `원장.md` 에서 절 뼈대를 세운다.
공개 목록 제목은 `자동화 시작하기` 이고 폴더 slug 는 `automation-start` 다.

흐름의 정본은 [원장.md](원장.md)다. 글을 쓰거나 순서를 바꾸기 전에 원장의 제목 사슬을 먼저 고친다.

## 이 폴더의 규칙

- 글 파일만 `NNN-kebab.md`다. `NNN`은 저장소 전체의 발행 순번이며 공개 읽기 순서가 아니다.
- 공개 URL은 frontmatter `slug`다. `/blog/{slug}`. 카테고리를 URL에 넣지 않는다.
- 목록 순서와 목록에 올릴지는 `blog/order.json`이 정한다.
- 다시 쓰기 전인 글은 파일과 URL을 지우지 않고 `archived`로 내린다.

## 지금 상태

네 편 다 발행했고 세트 1 이 닫혔다. 004 가 `진짜 엑셀 파일은 아직 안 열었다` 로 끝나며
세트 2 의 문을 연다.

**남은 흠.** 002 와 003 은 시각물 없는 절이 하나씩 있어 `memory/blogWriter.md` 의 관문 7 을
아직 못 넘긴다. 다음에 손볼 때 채운다.

다음 세트의 뼈대는 [원장.md](원장.md) 의 `세트 2. 내 엑셀 파일을 연다` 다.
